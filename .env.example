# Supabase Configuration (REQUIRED)
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Vercel KV (REQUIRED for rate limiting)
# Automatically set by Vercel when you add the KV integration
# For local development, create a KV database in Vercel dashboard
# https://vercel.com/docs/storage/vercel-kv/quickstart

KV_REST_API_URL=your-kv-url-here
KV_REST_API_TOKEN=your-kv-token-here
KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token-here

# Development Configuration (OPTIONAL)

# Enable debug logging (shows detailed console output)
NEXT_PUBLIC_DEBUG=false

# Node environment (automatically set by Next.js)
# NODE_ENV=development

# Redirect URL for authentication (local development)
# NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/dashboard

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Get your Supabase credentials:
#    - Go to https://supabase.com/dashboard
#    - Select your project
#    - Go to Settings → API
#    - Copy "Project URL" to NEXT_PUBLIC_SUPABASE_URL
#    - Copy "anon public" key to NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# 3. (Optional) Enable debug mode during development:
#    NEXT_PUBLIC_DEBUG=true
#
# 4. Save the file and restart your development server:
#    npm run dev
#
# 5. Run validation to check your setup:
#    npm run validate
#
# =============================================================================
# TROUBLESHOOTING
# =============================================================================
#
# Error: "Missing Supabase environment variables"
#   → Make sure you created .env.local (not .env)
#   → Verify the file contains valid Supabase URL and key
#   → Restart your development server after adding variables
#
# Error: "Failed to fetch" or API errors
#   → Check your Supabase project is active
#   → Verify the anon key has correct permissions
#   → Check your network connection
#
# For more help, see DEBUGGING_GUIDE.md
#
# =============================================================================
